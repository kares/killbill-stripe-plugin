:stripe:
  :api_secret_key: ''
  :api_publishable_key: ''
  :log_file: '/var/tmp/stripe.log'
  :test: true

:database:
#  :adapter: sqlite3
#  :database: test.db
# For MySQL
#  :adapter: mariadb
#  :username: root
#  #:username: killbill # TODO support <%=  %>
#  #:password: killbill # TODO support <%=  %>
#  #:driver: org.mariadb.jdbc.Driver
#  :url: jdbc:mysql://127.0.0.1:3306/killbill
# In Kill Bill
  :adapter: mysql
  :jndi: 'killbill/osgi/jdbc'
  :connection_alive_sql: 'select 1'
  :pool: false # false-pool (JNDI pool's max)
  #:retry_count: 5 # when obtaining from DS fails
  # MySQL adapter #configure_connection defaults :
  #    @@SESSION.sql_auto_is_null = 0,
  #    @@SESSION.wait_timeout = 2147483,
  #    @@SESSION.sql_mode = 'STRICT_ALL_TABLES'
  # this can be disabled since AR-JDBC 1.4 using :
  #:configure_connection: false
